on:
  push:
    branches:
      - slave





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Testing github Action'
        run: ls


      - name: 'Testing Printing'
        run: echo Tasneem

